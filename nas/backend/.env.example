# Backend container env (mounted via compose env_file)
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# JWT Authentication
JWT_SECRET=change-me
# Set to 'never' (or '0'/'none') to create non-expiring tokens
JWT_EXPIRY=7d

# Speech-to-Text
STT_PROVIDER=google
GOOGLE_STT_API_KEY=
# Optional: bounds STT calls to prevent indefinite hangs
STT_TIMEOUT_MS=15000
# Alternative: Local Whisper (OpenAI-compatible transcription endpoint)
# STT_PROVIDER=whisper
# WHISPER_BASE_URL=http://whisper:9000
# WHISPER_MODEL=base.en
# WHISPER_LANGUAGE=en
# Optional (only if your whisper service requires auth)
# WHISPER_API_KEY=

# LLM
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4

# Control4 MCP (c4-mcp HTTP server)
# Example (NAS compose): http://c4-mcp:3333
C4_MCP_BASE_URL=http://c4-mcp:3333
C4_MCP_TIMEOUT_MS=8000

# Optional: bounds LLM calls to prevent indefinite hangs
LLM_TIMEOUT_MS=15000

# Logging
LOG_LEVEL=info
CORS_ORIGIN=*
